spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:msfg_dashboard}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

logging:
  level:
    com.msfg.dashboard: INFO
