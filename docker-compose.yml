services:
  mysql:
    image: mysql:8.0
    container_name: msfg-java-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${DB_NAME:-msfg_dashboard}
      MYSQL_USER: ${DB_USER:-msfg_user}
      MYSQL_PASSWORD: ${DB_PASSWORD:-msfg_password}
    ports:
      - "3308:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - msfg-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: msfg-java-backend
    environment:
      SPRING_PROFILES_ACTIVE: docker
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-msfg_dashboard}
      DB_USER: ${DB_USER:-msfg_user}
      DB_PASSWORD: ${DB_PASSWORD:-msfg_password}
      JWT_SECRET: ${JWT_SECRET:-change-me-to-a-secure-random-string-at-least-64-chars-long-for-production}
      UPLOAD_DIR: /app/uploads
    ports:
      - "8083:8083"
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - msfg-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: msfg-java-frontend
    environment:
      VITE_API_URL: http://backend:8083
    ports:
      - "3007:3007"
    depends_on:
      - backend
    networks:
      - msfg-network

volumes:
  mysql_data:
  uploads_data:

networks:
  msfg-network:
    driver: bridge
